const path = require('path');

module.exports = {
  config: path.resolve(__dirname, 'dist', 'source', 'config', 'database.js'),
  'migrations-path': path.resolve(__dirname, 'source', 'database', 'migrations')
}